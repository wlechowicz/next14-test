export type Video = {
  backdrop_path: string;
  genres: Record<string, string>[];
  id: number;
  overview: string;
  poster_path: string;
  release_date: string;
  tagline: string;
  title: string;
};
